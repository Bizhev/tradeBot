// Позже это всегда исправляется
export type TODO_ANY = any;
export enum TradeStatusEnum {
    Process = 1, // В процессе
    End = 0, // закончен, завершил
};

export enum StrategyNameEnum {
    Invest = 'investing', // В процессе
    Test = 'test', // В процессе
};
